# Claude API Key (required if not using Claude Code app token)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Optional: path to Claude Code credentials (auto-detected if not set)
# CLAUDE_CODE_TOKEN_PATH=~/.claude/
